name: Cache
description: Persist cache in between build steps
author: https://github.com/lox, https://github.com/tombarton
requirements:
  - bash
  - rsync
configuration:
  properties:
    username:
      type: string
    server:
      type: string
  required:
    - username
